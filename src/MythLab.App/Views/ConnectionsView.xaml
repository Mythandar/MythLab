<UserControl x:Class="MythLab.App.Views.ConnectionsView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="8">
 <DockPanel><TextBlock Text="Connections &amp; credentials" FontSize="28" FontWeight="SemiBold"/>
 <Button Content="Refresh" Command="{Binding RefreshCommand}" HorizontalAlignment="Right"/></DockPanel>
 <TextBlock Text="{Binding Message}" Margin="0,12,0,16" AutomationProperties.LiveSetting="Polite"/>
 <TextBlock Text="SSH connections" FontSize="20" FontWeight="SemiBold"/>
 <DataGrid ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="170" Margin="0,8,0,8" SelectionMode="Single">
 <DataGrid.Columns><DataGridTextColumn Header="Connection" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="Device" Binding="{Binding DeviceName}" Width="*"/>
 <DataGridTextColumn Header="Credential" Binding="{Binding CredentialLabel}" Width="*"/><DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="70"/></DataGrid.Columns></DataGrid>
 <WrapPanel><Button Content="Add SSH" Command="{Binding AddProfileCommand}"/><Button Content="Edit" Command="{Binding EditProfileCommand}"/>
 <Button Content="Delete" Command="{Binding DeleteProfileCommand}"/><Button Content="Connect" Command="{Binding ConnectCommand}"/></WrapPanel>
 <TextBlock Text="Reusable credentials" FontSize="20" FontWeight="SemiBold" Margin="0,24,0,0"/>
 <TextBlock Text="Secrets stay in Windows Credential Manager. After moving the portable folder, use Edit / recreate to restore a missing secret." Opacity=".7" Margin="0,8,0,0"/>
 <DataGrid ItemsSource="{Binding Credentials}" SelectedItem="{Binding SelectedCredential}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="160" Margin="0,8,0,8" SelectionMode="Single">
 <DataGrid.Columns><DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*"/><DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
 <DataGridTextColumn Header="Type" Binding="{Binding Authentication}" Width="100"/><DataGridTextColumn Header="Windows secret" Binding="{Binding Availability}" Width="230"/></DataGrid.Columns></DataGrid>
 <WrapPanel><Button Content="Add credential" Command="{Binding AddCredentialCommand}"/><Button Content="Edit / recreate" Command="{Binding EditCredentialCommand}"/><Button Content="Delete" Command="{Binding DeleteCredentialCommand}"/></WrapPanel>
 <Expander Header="Trusted SSH hosts" Margin="0,24,0,0">
 <StackPanel><TextBlock Text="Changed keys are rejected. Verify changes independently before removing an old trust record." Margin="0,8"/>
 <DataGrid ItemsSource="{Binding KnownHosts}" SelectedItem="{Binding SelectedHost}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="150">
 <DataGrid.Columns><DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="*"/><DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="60"/><DataGridTextColumn Header="Fingerprint" Binding="{Binding Fingerprint}" Width="*"/></DataGrid.Columns></DataGrid>
 <Button Content="Remove selected trust" Command="{Binding RemoveHostCommand}" HorizontalAlignment="Left" Margin="0,8"/>
 </StackPanel></Expander>
 </StackPanel></ScrollViewer>
</UserControl>